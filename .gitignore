node_modules
dist
dist-embed-*
.firebase
*.log
.DS_Store
.env
.env.local

# Local-only Firebase / Genkit agent-skill cache pulled by some
# `firebase init` flows. Not project source code.
.agents/
skills-lock.json

# Playwright responsive-audit suite (tests/responsive/). Run on
# demand via `npm run test:responsive` — not in CI.
playwright-report/
test-results/
tests/responsive/screenshots/
