node_modules
dist
dist-embed-*
.firebase
*.log
.DS_Store
.env
.env.local

# Local-only Firebase / Genkit agent-skill cache pulled by some
# `firebase init` flows. Not project source code.
.agents/
skills-lock.json
